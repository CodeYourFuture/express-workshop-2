<article>
    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                <h1>{{post.title}}</h1>
                <h2>{{post.summary}}</h3>
                 <p>{{post.content}}</p>
            </div>
        </div>
    </div>
</article>